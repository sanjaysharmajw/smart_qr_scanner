name: smart_qr_scanner_example
description: Example app demonstrating smart_qr_scanner package features.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  smart_qr_scanner:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

dependency_overrides:
  camera_android_camerax: 0.6.5

flutter:
  uses-material-design: true
